# Certbot Stack

This Stack role configures certbot with the Cloudflare DNS challenge.

> [!NOTE]
> The snap installs via Ansible are flaky; don't be surprised if you need to run the playbook twice (with nothing changed) for it to work
